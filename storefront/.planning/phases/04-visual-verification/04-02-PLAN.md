---
phase: 04-visual-verification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - "components/cart/cart.module.css"
  - "components/cart/index.tsx"
  - "app/(main)/contact/page.tsx"
  - "components/product-highlight-carousel/index.tsx"
  - "components/product-highlight-carousel/product-highlight-carousel.module.css"
  - "styles/global.css"
autonomous: false
requirements:
  - VER-03
  - VER-04
  - VER-05
  - VER-06
  - VER-07

must_haves:
  truths:
    - "GSAP scroll-triggered animations fire on the homepage and product pages — elements fade/slide in as user scrolls"
    - "Lenis smooth scrolling is active — scroll has visible easing, not native jagged behavior"
    - "Embla carousels navigate correctly — arrow/dot clicks advance slides, auto-scroll works if enabled"
    - "Cart add/remove/update/open/close operations complete without errors and UI updates correctly"
    - "Contact form submits successfully with valid data and shows validation errors for invalid data"
    - "Hover states on buttons, nav links, product cards, and CTAs show visual feedback (color change, scale, opacity)"
  artifacts: []
  key_links:
    - from: "components/wrapper/index.tsx"
      to: "Lenis smooth scroll"
      via: "ReactLenis with autoRaf and Tempus"
      pattern: "ReactLenis"
    - from: "app/(main)/shop/[slug]/page.tsx"
      to: "Cart context add action"
      via: "useCartContext addItem"
      pattern: "addItem|useCartContext"
---

<objective>
Verify all interactive states — GSAP animations, Lenis smooth scrolling, Embla carousels, cart operations, contact form submission, and hover states/transitions — work correctly on the migrated site. Fix any regressions immediately. This is the final interactive verification gate before the milestone is complete.

Purpose: Layout verification (Plan 04-01) confirms static visual parity. This plan confirms dynamic behavior — the parts of the site that require user interaction to verify. Together, these two plans form the complete verification gate.

Output: All interactive states confirmed working. Any fixes committed. Milestone complete.
</objective>

<execution_context>
@/Users/armagansenol/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armagansenol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-verification/04-RESEARCH.md
@.planning/phases/04-visual-verification/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify GSAP animations, Lenis smooth scrolling, and Embla carousels</name>
  <files>
    components/wrapper/index.tsx
    components/product-highlight-carousel/index.tsx
    components/product-highlight-carousel/product-highlight-carousel.module.css
    styles/global.css
  </files>
  <action>
    Dev server should still be running from Plan 04-01. If not, start it: `pnpm dev` in background.

    **Lenis smooth scrolling verification:**
    1. Navigate to http://localhost:3000 at 1440x900 viewport
    2. Scroll down slowly using browser scroll or keyboard arrows
    3. Observe: scroll should have a smooth easing effect, not the native browser jump-scroll. Lenis adds visible momentum/deceleration.
    4. If scroll feels native/jagged: check browser console for Lenis errors using `browser_console_messages`. Look for errors from Tempus or ReactLenis initialization.
    5. Compare scroll feel against production: https://mightyfull-shop.vercel.app/

    **GSAP animation verification:**
    1. On homepage (http://localhost:3000), scroll down through the full page
    2. Observe: elements should animate into view as you scroll (fade-in, slide-in, scale effects)
    3. Key sections to watch: hero section, product highlights, customer reviews, follow-us section
    4. Check browser console for GSAP errors — any `GSAP target not found` warnings indicate broken selectors
    5. Compare animation behavior against production site
    6. On a product page (/shop/[slug]), verify any scroll-triggered section transitions

    **Embla carousel verification:**
    1. On the homepage, find the product highlight carousel
    2. Click carousel navigation arrows (left/right) — slides should advance
    3. If dots/indicators exist, click them — should jump to correct slide
    4. Verify carousel renders correctly at both 1440px and 375px viewports
    5. Compare carousel behavior against production

    **If animations/scroll don't work:**
    - Check browser console for errors
    - GSAP issues are typically broken CSS selectors (class names changed during SCSS→CSS migration)
    - Lenis issues are typically Tempus/RAF integration problems
    - Fix in the relevant component file, wait for HMR, re-verify
  </action>
  <verify>
    <automated>cd /Users/armagansenol/Desktop/justdesignfx/mightyfull-shop && pnpm build</automated>
  </verify>
  <done>GSAP scroll animations fire correctly on homepage and product pages. Lenis smooth scrolling is active with visible easing. Embla carousels navigate correctly with arrow/dot clicks. No console errors related to GSAP, Lenis, or Embla.</done>
</task>

<task type="auto">
  <name>Task 2: Verify cart operations, contact form, and hover states</name>
  <files>
    components/cart/cart.module.css
    components/cart/index.tsx
    app/(main)/contact/page.tsx
  </files>
  <action>
    **Cart operations (full end-to-end flow):**
    1. Navigate to http://localhost:3000/shop at 1440x900
    2. Click any product to open its product page (/shop/[slug])
    3. Click "Add to Cart" button — cart drawer should open
    4. Verify: cart item renders with product name, image, price, and quantity
    5. Update quantity: click + button — quantity number should increase, total should update
    6. Click - button — quantity should decrease
    7. Remove item: click remove/X button — item should disappear from cart, cart should show empty state or update count
    8. Close cart drawer — click X or click outside the drawer
    9. Re-open cart drawer via cart icon in header — verify it opens correctly
    10. If any step fails: check browser console for errors, inspect the cart component and context for issues

    **Contact form (full submission test):**
    1. Navigate to http://localhost:3000/contact
    2. Fill all fields: name ("Test"), surname ("User"), email ("test@example.com"), phone ("1234567890"), message ("Test message for verification")
    3. Submit the form
    4. Verify: success state appears (Sonner toast or success message)
    5. Test validation: refresh page, try to submit with empty required fields — verify error messages appear for each required field
    6. If submission fails: check browser console and network tab for API errors

    **Hover states and transitions (spot-check):**
    1. On homepage: hover over main CTA buttons — verify color/scale/opacity transition
    2. In header: hover over nav links — verify underline/color transition
    3. On /shop: hover over product cards — verify card hover effect (scale, shadow, or overlay)
    4. On product page: hover over "Add to Cart" button — verify button hover state
    5. In footer: hover over footer links — verify hover transition
    6. Compare hover behavior against production site for any differences
    7. Use `browser_hover` tool to trigger hover states, then take screenshots

    **If hover states are missing or wrong:**
    - Check the CSS module for `:hover` or `&:hover` selectors
    - In TW v4, hover: prefix should work normally
    - CSS module hover states may have been affected by SCSS nesting conversion — verify `&:hover` syntax is correct in native CSS nesting
  </action>
  <verify>
    <automated>cd /Users/armagansenol/Desktop/justdesignfx/mightyfull-shop && pnpm build</automated>
  </verify>
  <done>Cart add/remove/update/open/close operations work correctly. Contact form submits successfully and shows validation errors. Hover states on buttons, nav links, product cards, and CTAs display correct visual feedback. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human confirms all interactive states and signs off milestone</name>
  <files></files>
  <action>
    Present a complete summary of the entire Phase 4 verification:

    **Plan 04-01 results:**
    - Desktop (1440px) verification status for all 8 pages
    - Mobile (375px) verification status for all 8 pages
    - Fixes applied (if any)

    **Plan 04-02 results:**
    - GSAP animation status
    - Lenis smooth scrolling status
    - Embla carousel status
    - Cart operations status
    - Contact form status
    - Hover states status
    - Fixes applied (if any)

    **Final gate:**
    - `pnpm build` passes cleanly
    - No unresolved blockers

    Ask the user to confirm the milestone is complete. If there are remaining issues, document them as tech debt for a future milestone.
  </action>
  <verify>User confirms milestone is complete</verify>
  <done>User has signed off on the full visual and interactive verification. Phase 4 complete. Milestone v1.0 (stack upgrade + CSS migration) is finished.</done>
</task>

</tasks>

<verification>
- GSAP scroll animations fire on homepage and product pages
- Lenis smooth scrolling provides visible easing
- Embla carousels navigate via arrows and dots
- Cart drawer opens, shows items, allows quantity changes, allows item removal
- Contact form submits successfully and shows validation errors
- Hover states are visible on buttons, nav links, product cards, CTAs
- `pnpm build` completes without errors
</verification>

<success_criteria>
1. GSAP animations and scroll triggers fire correctly on all pages that use them
2. Lenis smooth scrolling is active with perceptible easing
3. Embla carousels navigate correctly
4. Cart add/remove/update/open/close complete without errors
5. Contact form submits successfully with valid data and shows errors for invalid data
6. Hover states and transitions match production baseline behavior
7. Clean `pnpm build` passes
8. User has signed off on the milestone
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-verification/04-02-SUMMARY.md`
</output>
