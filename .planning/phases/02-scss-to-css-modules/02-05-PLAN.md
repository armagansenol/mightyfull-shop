---
phase: 02-scss-to-css-modules
plan: 05
type: gap_closure
wave: 1
depends_on: [02-04]
files_modified:
  - components/header/header.module.css
  - components/footer/footer.module.css
  - components/auto-scroll-carousel/EmblaCarouselButtons.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CSS-01, CSS-02, CSS-03, CSS-04, CSS-05, CSS-06, CSS-07, CSS-08, CSS-09, CSS-10, CSS-11, CSS-12, CSS-13, CSS-14, CSS-15, CSS-16]

must_haves:
  truths:
    - "header.module.css .headerWrapper uses z-index: var(--z-header) instead of hardcoded 180"
    - "footer.module.css .footer uses z-index: var(--z-footer) instead of hardcoded 110"
    - "auto-scroll-carousel/EmblaCarouselButtons.tsx imports ./embla.module.css (not .module.scss)"
    - "REQUIREMENTS.md marks CSS-01, CSS-02, CSS-03, CSS-04, CSS-12 as [x] complete"
    - "pnpm build completes with zero errors after all fixes"
  artifacts:
    - components/header/header.module.css
    - components/footer/footer.module.css
    - components/auto-scroll-carousel/EmblaCarouselButtons.tsx
    - .planning/REQUIREMENTS.md
  key_links:
    - "header.module.css z-index references var(--z-header) from global.css :root (value 170)"
    - "footer.module.css z-index references var(--z-footer) from global.css :root (value 100)"
---

<objective>
Fix three verification gaps from Phase 02 completion: (1) replace hardcoded z-index values with var(--z-*) custom property references in header and footer modules, (2) fix stale .module.scss import in orphaned EmblaCarouselButtons.tsx, (3) update REQUIREMENTS.md to reflect actual completion state.

Purpose: Close all gaps identified in 02-VERIFICATION.md so Phase 02 achieves 5/5 verification truths.
Output: All four files updated, build passing.
</objective>

<execution_context>
@/Users/armagansenol/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armagansenol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-scss-to-css-modules/02-VERIFICATION.md
@.planning/REQUIREMENTS.md
@styles/global.css (lines 268-290 for z-index custom properties)
@components/header/header.module.css
@components/footer/footer.module.css
@components/auto-scroll-carousel/EmblaCarouselButtons.tsx
</context>

<tasks>
<task id="1" title="Fix z-index custom property references in header and footer modules">
<description>
Replace hardcoded z-index values with var(--z-*) custom property references in the two files where the original SCSS used z-index() function calls.

**header.module.css:**
- Line 9: Change `z-index: 180;` to `z-index: var(--z-header);`
- The value 180 was a miscalculation of the SCSS z-index('header') function. The correct custom property --z-header is defined as 170 in global.css line 282.
- IMPORTANT: Only the top-level `.headerWrapper` z-index (line 9) should use var(--z-header). The other z-index values in this file (10 on line 43, 200 on lines 65/73, 100 on line 89) are LOCAL stacking context values within the component — they were NOT using the z-index() SCSS function and should remain as hardcoded integers.

**footer.module.css:**
- Line 6: Change `z-index: 110;` to `z-index: var(--z-footer);`
- The value 110 was a miscalculation of the SCSS z-index('footer') function. The correct custom property --z-footer is defined as 100 in global.css line 289.
- IMPORTANT: Only the top-level `.footer` z-index (line 6) should use var(--z-footer). The other z-index values in this file (-1 on lines 21/220, 20/10/40/30 on lines 111/122/133/144, 100 on line 231) are LOCAL stacking context values for cookie/package elements — they were NOT using the z-index() SCSS function and should remain as hardcoded integers.
</description>
</task>

<task id="2" title="Fix stale .module.scss import and update REQUIREMENTS.md">
<description>
Two cleanup fixes:

**Fix 1 — Stale import in EmblaCarouselButtons.tsx:**
- File: `components/auto-scroll-carousel/EmblaCarouselButtons.tsx`
- Line 10: Change `import s from './embla.module.scss';` to `import s from './embla.module.css';`
- This file is orphaned (not imported by any component) but the broken import could cause TypeScript compilation issues since tsconfig.json includes all .tsx files.

**Fix 2 — Update REQUIREMENTS.md completion markers:**
- File: `.planning/REQUIREMENTS.md`
- Mark these 5 requirements as complete (change `[ ]` to `[x]`):
  - CSS-01: `_functions.scss` analyzed — functions/mixins documented with CSS equivalents
  - CSS-02: `_colors.scss` @each loop expanded into static :root CSS custom properties
  - CSS-03: z-index map extracted and converted to :root CSS custom properties
  - CSS-04: `global.scss` converted to `global.css` with all partials inlined
  - CSS-12: `@extend` in global.scss replaced with inlined properties
- Also update the Traceability table entries for CSS-01 through CSS-04 and CSS-12 from "Pending" to "Complete"

**Verification:** Run `pnpm build` after all changes. Build must complete with zero errors.
</description>
</task>
</tasks>

<verification>
After both tasks are complete:

1. **z-index var() references:** Grep `components/header/header.module.css` for `var(--z-header)` — must appear on the .headerWrapper rule. Grep `components/footer/footer.module.css` for `var(--z-footer)` — must appear on the .footer rule.
2. **No hardcoded z-index for header/footer top-level:** The values 180 and 110 must NOT appear as z-index values in these files.
3. **Import fix:** Grep `components/auto-scroll-carousel/EmblaCarouselButtons.tsx` for `.module.css` — must match. No `.module.scss` imports should exist.
4. **REQUIREMENTS.md:** All 16 CSS-* requirements should show `[x]` (complete).
5. **Build:** `pnpm build` exits with code 0.
</verification>
