---
phase: 05-css-custom-property-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/header/header.module.css
  - components/footer/footer.module.css
  - components/wrapper/wrapper.module.css
autonomous: true
requirements: [CSS-02, CSS-03, CSS-10, VER-07]
gap_closure: true

must_haves:
  truths:
    - "No undefined CSS custom property references remain in any .module.css file"
    - "pnpm build passes cleanly"
    - "Footer, header, and wrapper components render without CSS errors"
  artifacts:
    - path: "components/header/header.module.css"
      provides: "Header styles with no stale var() references"
    - path: "components/footer/footer.module.css"
      provides: "Footer styles with no stale var() references"
    - path: "components/wrapper/wrapper.module.css"
      provides: "Wrapper styles with no stale var() references"
  key_links:
    - from: "components/header/header.module.css"
      to: "styles/global.css :root"
      via: "All var() references resolve to defined custom properties"
      pattern: "var\\(--[a-z]"
---

<objective>
Remove 4 stale CSS custom property references from component modules. These var() calls reference custom properties that were intentionally removed or never defined, causing silent CSS failures (properties resolve to nothing).

Purpose: Close gaps flagged by v1.0 milestone audit — every var() in the codebase must resolve to a defined custom property.
Output: 3 cleaned CSS module files with zero undefined var() references.
</objective>

<execution_context>
@/Users/armagansenol/.claude/get-shit-done/workflows/execute-plan.md
@/Users/armagansenol/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-css-custom-property-gap-closure/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 4 undefined CSS custom property references</name>
  <files>
    components/header/header.module.css
    components/footer/footer.module.css
    components/wrapper/wrapper.module.css
  </files>
  <action>
Remove stale var() references from 3 CSS module files. Per locked decision: REMOVE references, do NOT add new definitions. Delete entire rules when they become meaningless without the var() value.

**header.module.css line 107** — `color: var(--laurens-lace);`
Located inside `.navItem > @media (max-width: 768px)`. This block also has `font-size: 25px;` which must be preserved. Remove ONLY the `color: var(--laurens-lace);` line. The parent `.navItem` already has `color: var(--primary)` at line 99, so mobile will inherit that color.

**header.module.css line 165** — `padding: var(--padding-x);`
Located inside `.navigationMenu > .categories { padding: var(--padding-x); }`. The `.categories` rule has ONLY this one property. Remove the entire `.categories { ... }` block (lines 164-166). The `.navigationMenu` parent remains with its other properties.

**footer.module.css line 204** — `color: var(--purple-cactus-flower);`
Located inside `.link > @media (hover: hover) > &:hover { color: var(--purple-cactus-flower); }`. The hover block has ONLY this color property. Remove the entire `@media (hover: hover) { &:hover { ... } }` block (lines 202-206). The `.link` rule retains its `transition: 200ms color ease;` which is harmless without a hover color change.

**wrapper.module.css line 14** — `z-index: var(--z-content);`
Located inside `.main`. Remove ONLY the `z-index: var(--z-content);` line. The remaining `.main` properties (flex-grow, position, display, flex-direction) are all meaningful.
  </action>
  <verify>
    <automated>cd /Users/armagansenol/Desktop/justdesignfx/mightyfull-shop && grep -rn "var(--laurens-lace)\|var(--padding-x)\|var(--purple-cactus-flower)\|var(--z-content)" components/ | grep -v node_modules; echo "Exit: $?"</automated>
  </verify>
  <done>Zero grep matches for the 4 undefined var() references across all component CSS modules</done>
</task>

<task type="auto">
  <name>Task 2: Verify build passes after CSS cleanup</name>
  <files>
    components/header/header.module.css
    components/footer/footer.module.css
    components/wrapper/wrapper.module.css
  </files>
  <action>
Run `pnpm build` to confirm the CSS changes do not break the build. All 3 modified CSS modules must be parsed without errors by PostCSS/Tailwind v4. If the build fails, diagnose and fix — the removals should be safe since these properties were already resolving to nothing.
  </action>
  <verify>
    <automated>cd /Users/armagansenol/Desktop/justdesignfx/mightyfull-shop && pnpm build 2>&1 | tail -5</automated>
    <automated>cd /Users/armagansenol/Desktop/justdesignfx/mightyfull-shop && echo "--- VER-07: Footer .link hover/transition check ---" && grep -n "transition" components/footer/footer.module.css && echo "--- Confirm hover block removed (no purple-cactus-flower) ---" && grep -c "purple-cactus-flower" components/footer/footer.module.css || echo "0 matches (expected)"</automated>
  </verify>
  <done>pnpm build exits 0 with no CSS-related errors or warnings. Footer .link retains `transition: 200ms color ease` property. Hover color block removed — intentional per CONTEXT.md (--purple-cactus-flower was never defined).</done>
</task>

</tasks>

<verification>
1. `grep -rn "var(--laurens-lace)\|var(--padding-x)\|var(--purple-cactus-flower)\|var(--z-content)" components/` returns zero matches
2. `pnpm build` exits 0
3. No new undefined var() references introduced
4. VER-07: Footer `.link` retains `transition: 200ms color ease` — hover color removal is intentional (--purple-cactus-flower was never defined per CONTEXT.md)
</verification>

<success_criteria>
- All 4 undefined CSS custom property references removed from component modules
- No remaining references to --laurens-lace, --padding-x, --purple-cactus-flower, or --z-content in any .module.css file
- pnpm build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-css-custom-property-gap-closure/05-01-SUMMARY.md`
</output>
